
# ========================================================
# Android APK 移动端依赖清单
# ========================================================
# 用途: PaddleOCR 文字识别工具 Android 版本
# 更新日期: 2025-01-XX
# ========================================================

# ========== 核心框架 ==========
kivy[base]>=2.3.0
python-for-android>=2023.03.12

# ========== PaddleOCR 核心 ==========
# 注意: 需要固定版本以避免兼容性问题
paddleocr>=2.7.0,<3.0.0
paddlepaddle>=2.6.0,<3.0.0

# ========== 图像处理 ==========
Pillow>=10.0.0,<11.0.0
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0

# ========== Android 平台支持 ==========
plyer>=2.1.0
pyjnius>=1.6.0

# ========== 其他工具 ==========
shapely>=2.0.0;<3.0.0
scipy>=1.10.0,<2.0.0
scikit-image>=0.21.0,<1.0.0
imgaug>=0.4.0,<1.0.0

# ========== 可选依赖 (按需启用) ==========
# tqdm  # 进度条显示
# PyYAML  # YAML 配置解析

# ========== 排除的包 (与 Android 不兼容) ==========
# tensorflow  # PaddleOCR 在 Android 上不需要 TensorFlow
# onnxruntime  # 仅桌面端需要

# ========================================================
# 重要说明:
# 1. paddlepaddle 在 Android 上可能需要特殊编译版本
# 2. opencv-python-headless 比 opencv-python 更轻量
# 3. 某些依赖可能需要在 buildozer.spec 中添加到
#    android.recipe 或通过自定义 recipe 编译
# ========================================================
